WORKCOUNT_HOST=127.0.0.1
WORKCOUNT_PORT=8765
WORKCOUNT_DB=data/workcount.db
# HTTPS deployment only (Docker enables this by default)
WORKCOUNT_SECURE_COOKIES=0
# Set to the public DNS name on a server with ports 80/443 open
WORKCOUNT_DOMAIN=workcount.example.org
